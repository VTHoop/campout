// Stub for the red commit (CAM-30).
import type { CalendarDate } from '@campout/planner';

export function shortMonthDay(_date: CalendarDate): string {
  return '';
}

export function longMonthDay(_date: CalendarDate): string {
  return '';
}
